pipeline { 
  
   agent any

   stages { 
     stage('Test') { 
        steps { 
           sh 'echo "testing application f..."'
        }
      }

         stage("Deploy application") { 
         steps { 
           sh 'echo "deploying application f..."'
           }
         }
   }
}
